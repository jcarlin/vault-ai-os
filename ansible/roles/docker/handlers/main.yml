---
# Docker Role - Handlers
# Service management handlers for Docker configuration changes

- name: Restart Docker
  systemd:
    name: docker
    state: restarted
    daemon_reload: yes

- name: Reload Docker
  systemd:
    name: docker
    state: reloaded
