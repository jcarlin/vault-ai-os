# Vault AI Backend - Environment Configuration
# Managed by Ansible — do not edit manually
# Maps to app/config.py Settings class

# vLLM inference backend
VLLM_BASE_URL={{ vault_vllm_base_url }}

# Security
VAULT_SECRET_KEY={{ vault_secret_key_actual }}

# Database
VAULT_DB_URL={{ vault_db_url }}

# Logging
VAULT_LOG_LEVEL={{ vault_backend_log_level }}

# Model manifest
VAULT_MODELS_MANIFEST={{ vault_backend_config_dir }}/models.json

# CORS
VAULT_CORS_ORIGINS={{ vault_cors_origins }}

# Setup wizard
VAULT_SETUP_FLAG_PATH={{ vault_backend_data_dir }}/.setup_complete

# TLS certificates
VAULT_TLS_CERT_DIR={{ vault_backend_tls_dir }}

# Deployment mode
VAULT_DEPLOYMENT_MODE={{ vault_deployment_mode }}

# Model management
VAULT_MODELS_DIR={{ vault_backend_models_dir }}
VAULT_GPU_CONFIG_PATH={{ vault_backend_config_dir }}/gpu-config.yaml
VAULT_VLLM_CONTAINER_NAME={{ vault_vllm_container_name }}

# Quarantine pipeline
VAULT_QUARANTINE_DIR={{ vault_backend_quarantine_dir }}

# Dataset management (Epic 22)
VAULT_DATASETS_DIR={{ vault_backend_datasets_dir }}

# Update mechanism (Epic 10)
VAULT_UPDATES_DIR={{ vault_updates_dir }}
VAULT_GPG_PUBLIC_KEY_PATH={{ vault_gpg_key_path }}
VAULT_VERSION_FILE_PATH={{ vault_version_file }}
